[package]
name = "rux-verify"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
proptest = "1.5"

# Note: kani is injected by `cargo kani` at verification time, not listed as a dependency.
# It is available via #[cfg(kani)] gates only when running `cargo kani`.
