# Rux 内核第三轮审查 — 修复计划（2026-09-12）

> 输入：[code-review-2026-09-12.md](code-review-2026-09-12.md)（第三轮全面审查，约 440 项独立发现）
> 参考基准：Linux 6.x（/home/william/refer/linux/）、virtio 1.1 spec、RFC 793/1122、POSIX
> 历史轮次：2026-04-15（第一轮）、2026-04-17（第二轮，F 系列 + Batch G–V）
> 本文档用途：作为后续修复工作的唯一跟踪入口。每完成一项请更新状态列并按批次提交 commit。

## 状态标记

| 标记 | 含义 |
|---|---|
| ☐ | 待修复 |
| 🔧 | 进行中 |
| ✅ | 已修复（需注明验证方式） |
| ⏸ | 暂缓（必须注明原因与风险） |

## 工作量标记

S = 半天内 ｜ M = 1–2 天 ｜ L = 3–5 天 ｜ XL = >1 周

---

## 1. 执行摘要

第三轮审查统计（去重后）：

| 严重级别 | 数量 | 说明 |
|---|---|---|
| Critical | 34 | 挂死/panic、静默数据损坏、提权、物理内存 UAF |
| High | ~110 | 用户可见错误行为、概率性挂死/UAF、POSIX/ABI 违反 |
| Medium | ~185 | 边界/语义偏差、权限缺失、竞态窗口 |
| Low | ~135 | 细节、性能、一致性 |

六大主题（按风险集中度）：

1. **底层上下文切换与异常处理不可靠**（ARCH-C1/C2）——所有"概率性"症状的疑似根因。
2. **用户输入到达内核核心路径缺乏防御**（分配大小、ELF 边界、裸用户指针、fd 类型）——一步挂死/提权。
3. **网络栈收发双向从未真正工作**（NET-C1/C2/C3/C4）——前两轮"已修复"未经真实收发验证。
4. **ext4 常规操作静默毁盘**（EXT4-C2/C3/C4）+ 页生命周期系统性泄漏（munmap 不 put_page、swap slot UAF）。
5. **等待/唤醒原语大面积失效**（futex >8 不唤醒、semop 语义反转、超时永不生效、vfork 丢唤醒）——多线程与 shell 场景挂死。
6. **测试与文档可信度**（SPIN LTL 未验证、单测无 CI、假测试、kernel-lock.md 等文档描述已删除机制）。

建议节奏：**Wave 1 先行**（1 周内，止血）；Wave 2 紧随（安全与数据完整性底线）；Wave 3/4 恢复网络与驱动可用性；Wave 5–7 按模块推进语义；Wave 8/9 与各波并行开展治理。总量约 **35–50 人天**。

---

## 2. 修复批次总览

| 批次 | 主题 | 项数（C/H/M） | 工作量 | 里程碑目标 |
|---|---|---|---|---|
| Wave 1 | 止血：消除一步挂死/必现 panic | 11 项（C8/H2/M1） | ~5 人天 | 坏指针/坏 ELF/坏镜像不再击杀内核 |
| Wave 2 | 数据损坏与安全底线 | 26 项（C17/H9） | ~15 人天 | 权限体系生效；无静默内存/文件系统损坏 |
| Wave 3 | 网络栈恢复 | 10 组（C4/H6/M15/L12） | ~10 人天 | loopback + slirp 下 TCP/UDP 端到端可用 |
| Wave 4 | 驱动修复 | 9 组（C2/H4/M8/L9） | ~6 人天 | PCI virtio-blk 中断路径正常；MMIO 设备可用 |
| Wave 5 | 进程/调度/信号语义 | 20 组（C4/H7/M17/L7 + 信号 H/M） | ~8 人天 | LTP 进程/信号/调度用例显著改善 |
| Wave 6 | 内存语义完善 | 12 组（H7/M12/L8） | ~6 人天 | fork/COW/swap/mprotect 语义正确 |
| Wave 7 | ext4 加固 + syscall 语义扫尾 | ~40 项（C0/H8/M33/L19） | ~8 人天 | 恶意镜像免疫；syscall 语义对齐 |
| Wave 8 | 测试与 CI 治理 | 9 项（C3/H6）+ 补测 | ~4 人天 | 单测进 CI 门禁；假测试清零 |
| Wave 9 | 文档治理 | 53 项 + 36 链接 | ~3 人天 | 文档与代码一致；数字脚本化 |

> 注：Wave 2 计入的 Critical 含与 Wave 1 重叠项的去重；各表内 ID 与检视报告一一对应。

---

## 3. Wave 1 — 止血：消除一步挂死与必现 panic（优先级最高）

**目标**：任何非特权用户的一行代码（坏指针 syscall、坏 ELF、坏镜像挂载、坏 futex 地址）都不再能击杀或挂死内核。

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 1.1 | ARCH-C1 | arch/riscv64/uaccess.S + exception.rs | EXTABLE 标签系统性错位一条指令，copy_from_user/clear_user 故障指令未被异常表覆盖 | 把每处 `EXTABLE .Lx, fixup` 移到被覆盖指令之前并让标签落在该指令上（Linux 向后引用风格）；修完写校验脚本比对 .ex_table 表项与反汇编访存指令一一对应 | M | ✅ 已修复：26 个表项全部对齐访存指令（objdump 实证）；新增 `scripts/verify_exception_table.py` 校验脚本（26 项 0 错误）；linker.ld `.ex_table` ALIGN 4→8 |
| 1.2 | ARCH-C2 | arch/riscv64/trap.S | trap 入口在保存 t0/t1/t2 前将其 clobber（用户与内核路径均中招） | 仿 Linux：进入后第一时间仅以 `sd xN, PT_xN(sp)` 保存全部 GPR；ti_kernel_sp 读取、地址规范检查、GET_PER_CPU_INTR_STACK、栈界检查全部移到保存之后 | M | ✅ 已修复：分发改用 beqz/bltz（零临时寄存器）；内核路径在当前 sp 立即保存全部 GPR（无栈切换，嵌套 trap 自然向下嵌套，同时消除 M-2 覆盖问题）；用户路径经 ti_user_sp 暂存后保存；二进制与 QEMU 启动/交互实测通过 |
| 1.3 | SYSA-C1 + IOU-H3 | syscall/io.rs:83,145,250,774；syscall/file.rs:281；io_uring/mod.rs:587,621 | 用户可控 count/len 直接 `vec![0u8; n]` → alloc 失败 panic | 统一 `count = min(count, MAX_RW_COUNT)`（2GB-4K）；getdents 截断到 4MB；io_uring 的 SQE len 同样钳制 | S | ☐ |
| 1.4 | PROC-P01 + VFS-H4/H5 + INIT-M10 | process/exec.rs:177；fs/elf.rs:405,639；init.rs:267 | ELF p_offset/p_filesz 与解释器 e_phoff 无边界校验 → slice 越界 panic / OOB 读 | 三处入口统一前置校验：`p_offset.checked_add(p_filesz) <= data.len()`、`e_phoff + e_phnum*56 <= len`，失败返回 ENOEXEC | S | ☐ |
| 1.5 | syscallb-C01 | syscall/process.rs:2526-2540 | riscv_hwprobe 在 S 态读 M 态 CSR → 非法指令 → 死循环挂死 | 删除 csrr mvendorid/marchid/mimpid，改返回常量/0；同时给 handle_illegal_instruction 增加内核态兜底（panic 打印 epc 而非静默循环，联动 ARCH-M1） | S | ☐ |
| 1.6 | syscallb-H13 + IPC-C5 + IOU-H4 | ~30 处（清单见检视报告 §6/§11/§12） | access_ok 后裸解引用用户指针 → 未映射页触发内核态缺页 → panic 挂死 | 全部改 copy_from_user/copy_to_user（依赖 1.1 完成后才真正 fault-safe）；优先修 futex/syslog/setitimer/epoll/times/bind | M | 🔧 部分完成：futex×2、syslog×2（改内核缓冲+copy_to_user+256KB 上限）、setitimer、times、bind/connect、sigprocmask×2、sigaction×2 已修；其余约 15 处（检视报告 §6 H-13 清单）留待后续批次 |
| 1.7 | EXT4-H2 | fs/ext4/mod.rs:171-187,267-282 | 超级块几何零校验：blocks_per_group=0、desc_size>块长、inode_size=0 等多处除零 panic | init 集中校验：`blocks_per_group ∈ (0, 8*block_size]`、`inode_size ∈ [128, block_size]`、`desc_size ∈ [32, block_size]`、block_size==4096（联动 7.1），坏值拒绝挂载返回 EIO | S | ☐ |
| 1.8 | EXT4-C1 | fs/ext4/mod.rs + extent.rs:158-236 | extent 树 eh_max/eh_entries 采信磁盘值（OOB 读数百 KB）+ 递归无深度上限（栈溢出） | 内部节点上限 (60-12)/12、外部节点上限 (block_size-12)/12；递归传 depth 参数限制 ≤5 | S | ☐ |
| 1.9 | MM-C1 | mm/page_desc.rs:547-551 | copy_page_contents 裸物理地址解引用 → compaction 触发即 panic | `src/dst = phys_to_virt(pfn_to_phys(pfn))` | S | ☐ |
| 1.10 | syscallb-H11 | syscall/misc.rs（epoll/timerfd/ioctl） | 不校验 fd 类型即转换 private_data → 类型混淆内存破坏 | FileOps 增加 ops 指针比对（`file.get_ops() as *const _ == &EPOLL_OPS`），不匹配返回 EINVAL；io_uring_enter/register 同模式（联动 2.5） | S | ☐ |
| 1.11 | TEST-H6 | .github/workflows/*.yml | CI 无 pipefail，编译失败也绿 | run 块加 `set -o pipefail`；spin Makefile 在 FAIL/ERR>0 时 `exit 1` | S | ☐ |

**验收标准**：
- 新增回归测试：坏用户指针（NULL、munmap 后地址）跑 read/write/getdents/execve/futex/syslog/times 不 panic，返回 EFAULT；
- 构造 p_offset 越界 ELF、eh_entries=65535 镜像、blocks_per_group=0 镜像：execve 返回 ENOEXEC、挂载返回 EIO；
- `make build && make test && make run`（smoke 15 项）全通过。
- 特别回归：ARCH-C2 修复后观察历史"概率性"崩溃场景（SMP 启动、LTP 长跑）是否消失。

**完成记录（2026-09-12）**：全部 11 项完成（1.6 为优先子集）。验证结果：
- `make build` 与 `make -C build build-release` 双 profile 通过；异常表校验脚本 26 项 0 错误。
- QEMU 4 核 SMP 启动到 mrsh shell；交互测试 `echo`/`ls /` 通过（覆盖修复后的 copy_from/to_user、getdents、ext4 挂载校验路径）。
- 顺带修复：unit-test 特性构建的 13 处测试代码类型错误（遗留破损，`make test` 此前完全编不过）；unit-test 运行时在 tests 环境仍有遗留内核态 fault（经 git stash 对照确认为**改动前已存在**，症状完全一致），归入 Wave 8 测试治理；同时消除 ARCH-M1（内核态异常静默跳过，现为 panic）与 M-2（嵌套 trap 覆盖中断栈顶，内核路径不再切换栈）。

**掩盖关系**：1.6 依赖 1.1（异常表修复后裸解引用才可能只返回 EFAULT 而非 panic）；1.1 完成后 1.5 的死循环自然变成可控异常。

---

## 4. Wave 2 — 数据损坏与安全底线

**目标**：权限体系生效；消除静默数据损坏（内存、文件系统）与提权路径；等待/唤醒原语可用。

### 4A. 安全

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 2.1 | VFS-C1 | fs/vfs.rs:1111-1199, 347-489 | open()/路径遍历零 DAC 检查 | path_lookup 每目录组件 MAY_EXEC；file_open/opendir 按 O_ACCMODE 做 generic_permission；read/write 按 f_mode 拒绝（O_RDONLY 写返回 EBADF） | M | ✅ 已修复：path_lookup 每目录 MAY_EXEC、file_open 按 O_ACCMODE、O_CREAT 父目录 W+X、file_opendir MAY_READ、File::read/write f_mode 强制（O_RDONLY 写/O_WRONLY 读 → EBADF）；新增 permission::inode_permission 辅助 |
| 2.2 | syscallb-C02 + PROC-P11 | syscall/process.rs:265-352；process/exec.rs:519 | execve 加载失败前提交 setuid 凭据（提权）+ AT_SECURE 恒 0 + LD_PRELOAD 不清洗 + root 降 uid 不清 caps | 凭据在栈上构造、do_execve_elf 成功后一次性提交；secureexec 标志传递写 AT_SECURE=1 并剔除 LD_*/IFS；setuid 系列在 euid 离开 0 时清 cap_effective/permitted | M | ✅ 已修复：凭据先提交后可回滚（失败恢复旧值）；AT_SECURE 正确置位 + LD_* 清洗；AT_UID/GID/EUID/EGID 取真实凭据；root exec 保留 caps（pP|=bset, pE=pP）；setuid/setreuid/setresuid 离开 euid 0 清 caps；execve 失败唤醒 vfork 父进程 |
| 2.3 | VFS-H1 同源 | syscall/file.rs:947-976 | dirfd 完全被忽略（*at 退化为 CWD） | resolve_user_path 对 dirfd≠AT_FDCWD 经 get_file_fd 取 dentry 拼绝对路径；fd 无效返回 EBADF | M | ✅ 已修复：dirfd 相对路径经 fd→dentry→build_path 解析；无效 dirfd → EBADF、非目录 → ENOTDIR；file_open/opendir 接线 dentry（连带修复 M-10 anon_inode 显示） |
| 2.4 | SYSA-H4 + VFS-H3 | syscall/file.rs:141；ipc/posix_mq.rs:816 | close(fd≥512) 被 MQ 劫持，普通 fd 无法关闭 | sys_close 先查进程 FdTable，命中走普通路径；仅 FdTable 无此 fd 且 fd∈[512,576) 才走 MQ | S | ✅ 已修复：sys_close 先查进程 fd 表，命中走普通路径；仅表中无此 fd 且 fd∈[512,576) 才走 MQ |
| 2.5 | IOU-C1 | io_uring/mod.rs:852-893 | enter/register 无 ops 校验的类型混淆（任意内核读写） | 复用 close/mmap 的 `IO_URING_OPS` 指针比对模式，不匹配返回 EOPNOTSUPP | S | ✅ 已修复（随 Wave 1.10 提前完成） |
| 2.6 | IOU-H1/H2 | io_uring/mod.rs + fs/file.rs | enter/close 并发 ring UAF + ring 物理页永不释放 | private_data 改 `Arc<IoUring>`；实现 Drop 释放三块 ring 区域；close 只在最后引用时释放 | M | ✅ 已修复：IoUring 引用计数（try_ref/unref + RingRef RAII），enter/mmap 持引用防 close 并发 UAF；impl Drop 释放三块 ring 物理页 |
| 2.7 | IOU-H5 + M-05 | printk.rs:296-355；io_uring/mod.rs:896 | syslog 无 CAP_SYSLOG 检查 + eventfd 注册不持引用 | action 2/4/5/6/7/8/9 前加 capable(CAP_SYSLOG)；eventfd_fd 改存 Arc\<File\> | S | ✅ 已修复：syslog 除 size 查询外全部要求 CAP_SYSLOG；eventfd 注册持 Arc<File> 引用并校验 EVENTFD_OPS 类型 |
| 2.8 | syscallb-H01/H02/H04 + PROC-P21 | syscall/process.rs:520-560；syscall/signal.rs:434；signal.rs:1232 | kill(0)/kill(-pgid)/tkill/前台组播均无权限检查；kill(-1) 被当作组 1 | 组播循环内补 can_send_signal 并传播 EPERM；kill(-1) 特判遍历全部任务（跳过 self 与 pid 1）；tkill 补权限检查；遍历改 pid_hash（睡眠进程也能收到） | M | ✅ 已修复：kill(0)/kill(-pgid)/kill(-1) 逐目标 can_send_signal 检查（EPERM 传播）；kill(-1) 正确跳过 self/pid1；广播改 pid_hash_for_each_task（睡眠任务可收）；tkill 补权限检查（含 sig==0）；TTY 前台组播同样补检查 |

### 4B. 静默数据损坏（内存）

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 2.9 | SYSA-C2 | syscall/memory.rs:707-739 + mm_ops.rs:82 | mremap 搬移一页都拷不到，新区域变零页 | copy_old_to_new_pages 改为：walk 旧地址取 PTE → 直接移植 PTE 到新地址（或临时映射新物理页拷贝后重填）；修后回收旧 PTE 引用 | M | ✅ 已修复：copy_old_to_new_pages 改为急切分配+映射+拷贝（镜像缺页路径的 rmap 记账），不再依赖懒分配的新地址 PTE |
| 2.10 | SYSA-C3 | syscall/memory.rs:67-84 | brk 收缩无 heap_start 下限，可解除代码段映射 | `if new_brk < start_brk { return current_brk; }`（start_brk 取自 address space 而非硬编码） | S | ✅ 已修复：收缩下限 start_brk（低于下限忽略并返回当前 brk，Linux do_brk 语义） |
| 2.11 | MM-C3 + MM-H3 | mm/vmscan.rs:180-291 | 共享页 swap-out 释放仍被引用的 slot（swap 数据交叉污染）+ 回收无 pin | 候选过滤 `refcount==1 && mapcount==0`；隔离时 try_get_page pin、完成/放弃对称 put；写 swap 前后校验 refcount/mapcount 未变 | L | 🔧 部分完成：候选过滤与 TOCTOU 复查加 refcount==1（独占），部分 unmap/意外 refcount 路径改为泄漏 slot 而非 UAF 释放——slot UAF 已闭合；完整页锁 pin 机制留待后续（MM-H3 剩余部分） |
| 2.12 | MM-C2 | mm/compact.rs:293-424 | 迁移后 remap_page 以有效 PTE 为门控但 PTE 已清零 → 数据变零页 | unmap 前保存旧 PTE 权限位，remap 直接写 `(new_ppn<<10)|saved_flags|V`，不做 walk 门控 | M | ✅ 已修复：unmap 前保存 PTE 权限位；remap_page 去掉 walk 门控（以页表层级存在性为门控）重建 V|flags；并修正 migrate_page 把页号当 vaddr 的叠加 bug（index×PAGE_SIZE） |
| 2.13 | MM-H2 | mm/page_alloc.rs + zone.rs | buddy 描述符状态迁移在 zone 锁外（SMP 空闲链表损坏） | 整块 2^order 个描述符的 refcount/order 设置移入 zone 锁临界区（alloc_from_order/free_pages 内完成） | M | ✅ 已修复：块描述符 refcount/mapcount/flags/order 状态迁移全部移入 zone 锁内（alloc_pages/free_pages 临界区）；外层重复循环删除 |
| 2.14 | ARCH-H3 + MM-H4 | arch/riscv64/mm/mm_ops.rs:302-447 | munmap/MAP_FIXED 只清 PTE 不 put_page（页泄漏） | unmap_pages 内取 ppn 后 `put_page()`，归零即 free_pages；覆盖 MAP_FIXED 清除循环 | S | ✅ 已修复：unmap_pages 与 MAP_FIXED 清除循环补 put_page（mapcount+引用成对释放，最后一个引用 free_pages） |
| 2.15 | ARCH-H4 | process/exit.rs:110-201 + mm/mm_struct.rs | do_exit 在仍用该页表时释放根页表（真 SMP 必炸） | 释放 mm 前先把本 CPU satp 切到内核根页表（复用 context.rs kernel_ppn 逻辑）；或延迟释放到上下文切换之后（mmdrop 模式） | M | ✅ 已修复：do_exit 释放地址空间前切换 satp 到内核根页表 + sfence（复用 context_switch 模式） |
| 2.16 | IPC-C4 | ipc/sysv_shm.rs:519-601 | shmat 与 IPC_RMID 竞态：物理页被释放后 PTE 仍映射（内存 UAF） | shmat 在同一锁段内先 nattch+=1 再映射；RMID 的计数-释放判定与 attach 的计数-映射同临界区 | S | ✅ 已修复：nattch 在与 RMID 同一 slots 锁段内先行递增（所有失败路径对称回退） |

### 4C. 等待/唤醒原语

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 2.17 | IPC-C1 | ipc/sysv_sem.rs:568-697 | semop 阻塞语义反转（P 返回 EINVAL；IPC_NOWAIT 反而阻塞） | try_apply_semops 需阻塞时返回专用哨兵；semtimedop 对哨兵进入阻塞分支、对 -EAGAIN 直接返回 | S | ✅ 已修复：SEMOP_NEED_BLOCK 哨兵（errno 区外）；IPC_NOWAIT 的 EAGAIN 直接返回用户；阻塞分支正确进入 |
| 2.18 | IPC-C3 + M7 | sync/futex.rs:158-212,516-674 | WAKE/REQUEUE 超 8 个等待者静默丢弃（cond_broadcast 批量挂死）；requeue_list 固定 32；负数参数不 EINVAL | wake_list/requeue_list 改动态 Vec（syscall 上下文可分配）；nr_wake/nr_requeue 负值返回 EINVAL | M | ✅ 已修复：wake_list 改动态 Vec（wake/requeue 两处）；负数 nr_wake/nr_requeue 由 Vec 条件自然限制（EINVAL 语义留 Wave 7） |
| 2.19 | IPC-C2 + H4 | sync/futex.rs:111-119,275-339 | waiter 池双分配（链表自环）+ 唤醒后 slot UAF | alloc_waiter 持锁期间写入占位后再返回；futex_wake 只置 woken+摘链不 free，等待者醒来自查所有权后再 free | M | ✅ 已修复：alloc_waiter 持锁写入占位（消除双分配窗口）；唤醒者不再释放 slot——等待者醒后按 task 指针验证所有权并自行释放/摘链 |
| 2.20 | IPC-H3 | sysv_sem/posix_mq/futex | 四个带超时 API 永不超时（永久挂起） | 阻塞前 add_timer_wakeup(deadline)，醒来后循环自然返回 ETIMEDOUT；futex_wait 增加 timeout 参数传递 | M | ✅ 已修复：semtimedop/mq_timedsend/mq_timedreceive/futex(WAIT/WAIT_BITSET) 全部装/卸 add_timer_wakeup 定时器；futex timespec 解析为 jiffies deadline |
| 2.21 | PROC-P02 | process/fork.rs:381-397 | vfork 双丢唤醒（父进程永久 D 状态） | set_vfork_parent 移到 enqueue_task 之前；父进程置 UNINTERRUPTIBLE 后复查子进程是否已 exec/exit 再 schedule（或引入 completion） | S | ✅ 已修复：set_vfork_parent 先于 enqueue_task；置 UNINTERRUPTIBLE 后复查 vfork_parent 已清则跳过睡眠（prepare-to-wait）；exec 失败也唤醒父进程 |
| 2.22 | PROC-P04 | sched/sched.rs:746-763 | 994b306 回归：deferred exit notify 读错 CPU 槽位，SIGCHLD 可无限延迟 | 用 context.rs 已有 per-CPU prev 记录取退出任务 parent pid，消除 CPU 推断；恢复切换后读 cpu_id() 亦可 | S | ✅ 已修复：切换后重新读 cpu_id()（tp→next.ti_cpu=硬件 CPU），恢复 994b306 误改的语义 |
| 2.23 | VFS-H6 + M14 | fs/pipe.rs:227-358 | pipe 阻塞读/写不检查 signal_pending（进程不可 SIGKILL）+ SMP 丢唤醒窗口 | schedule() 返回后检查 signal_pending 返回 -EINTR；prepare_to_wait 后重查条件再睡 | S | ✅ 已修复：pipe 读/写循环 schedule 后检查 signal_pending（EINTR/部分写返回）；prepare_to_wait 后复查条件消除 SMP 丢唤醒窗口 |
| 2.24 | syscallb-H05 + IPC-M14 | syscall/signal.rs:75-95 + sigsuspend | SIGKILL/SIGSTOP 可被 sigprocmask/sigsuspend 屏蔽（不可杀进程） | result_mask 计算后统一 `& !((1<<8)|(1<<18))`；sigsuspend 读入时同清洗 | S | ✅ 已修复：sigprocmask/sigsuspend 统一剥离 bit8/bit18（SIGKILL/SIGSTOP） |
| 2.25 | syscallb-H15 + IPC-H6 | signal.rs:779-781 + exec.rs:55 | sigframe 门控：longjmp/exec 跳出后进程永久"失聪"（含不可 SIGKILL） | 最小修复：do_exit 与 execve 清 sigframe；setup_frame 记录投递时 sp/epc，do_signal 检测 sp 离帧即清除；中期改用户栈帧链（Linux 方案） | M | ✅ 已修复（最小集）：exec/do_exit 清 sigframe+sigframe_addr；do_signal 增加 sp 离帧检测（longjmp 跳出后自动解除门控，进程恢复可杀） |
| 2.26 | IPC-H8 | interrupt/ksoftirqd.rs:41-104 | ksoftirqd 丢唤醒（置态后不复检 + WAKE 折叠吞唤醒） | 置 INTERRUPTIBLE 后复检 has_pending_softirqs，非空恢复 RUNNING；去掉 swap 折叠或折叠失败也 wake_up | S | ✅ 已修复：置 INTERRUPTIBLE 后复查 has_pending_softirqs，非空恢复 RUNNING 继续 drain |

### 4D. VFS 正确性（本轮一并修复的 High）

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 2.27 | VFS-H7 | fs/bio.rs:686-776 | bread_async 缓存命中不完成调用者 completion → bread_wait 永久挂起 | 缓存命中（含双检）分支立即 complete(0)；在飞时挂到既有 I/O 完成回调 | S | ✅ 已修复：Phase 1 纯缓存命中路径 complete(0)（无 I/O 在飞）；Phase 3 双检路径不补 complete（自身 I/O 会触发） |
| 2.28 | VFS-H8 | fs/inode.rs icache + rootfs/procfs/devfs iget | icache 无 fs 隔离：/proc/[小pid] 顶替 /etc 等同号 inode | 三个 fs 各分配唯一 fs_id 常量（或用 superblock 指针）；或桶匹配时校验 ops 表指针 | S | ✅ 已修复：rootfs/procfs/devfs 分别打 FS_ID_* 常量（12 处 inode 创建点），与 ext4 指针型 fs_id 不冲突 |
| 2.29 | VFS-H2 | fs/vfs.rs:460-476 | LOOKUP_NOFOLLOW 在缓存未命中分支失效（lstat 结果随缓存变） | None 分支的 follow_symlink 改为与 Some 分支相同的 `is_last && NOFOLLOW` 条件 | S | ✅ 已修复：None 分支与 Some 分支一致跳过 follow（第四轮复查 FIX-PASS） |
| 2.30 | VFS-M1 | fs/vfs.rs:898-925 | rename/link 无 EXDEV 检查（跨 fs 类型强转 → 内存破坏） | vfs_rename/link 前比较两 parent 的 ops/superblock，不同返回 EXDEV | S | ✅ 已修复：比较 inode ops 指针（第四轮复查 FIX-PASS；注意 ops 相等只区分类型不区分实例，双 ext4 挂载时需升级为 sb 指针） |
| 2.31 | SYSA-H2 同源 | syscall/file.rs:464-517 | readlink 普通符号链接 ENOENT | 增加通用分支：path_lookup(NOFOLLOW) → inode.op_readlink | S | ✅ 已修复：通用 readlink 分支落地（第四轮复查 FIX-PASS；遗留 dirfd 忽略与 /proc/N/fd 越界 panic，见 2R.4/2R.5） |

**完成记录（2026-09-13）**：全部 25 项完成（2.11 为 slot-UAF 闭合 + pin 机制部分）。验证：双 profile 构建通过、异常表 26/0、QEMU 4 核 SMP 启动到 shell、`echo`/`ls`/`cat` 交互通过（root 的 CAP_DAC_OVERRIDE 正确放行全部 DAC 检查）。遗留说明：① mrsh 管道（`echo|head`）报 EBADF 经 A/B 对照确认为 Wave 2 之前即存在的遗留缺陷（fork 子进程 fd 传递问题，非本轮引入，待后续 wave 追查）；② /dev/null 未注册进 devfs（devfs 仅 kmsg/input），`2>/dev/null` 失败为既有行为，归入 Wave 7 devfs 范畴。

**⚠ 第四轮复查（2026-09-13）核验结果**：25 项中 2.2（凭据回滚时机错误）、2.6（引用计数生命周期）、2.16（RMID 释放窗口）、2.25（离帧检测条件反向）、2.20（futex 无 ETIMEDOUT）、2.27（在飞脏读）、2.14（put_page 对 shm/ring 无记账映射成灾）共 **7 项失败或不完整**——详见检视报告 §16.1 与下方 Wave 2R。

---

## 3R. Wave 2R — 回归热修：Wave 1/2 失败项 + 新 Critical（2026-09-13 第四轮复查产出）

**目标**：闭合提权链（凭据回滚、内核地址映射）、一步 panic（readlinkat、read/write 大 count）、Wave 2 引入的 UAF 回归（shm/ring 页 vs put_page、io_uring 生命周期）。优先于 Wave 3 执行。

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 2R.1 | C02-FIX + NEW | syscall/process.rs:354 | cred_saved 克隆于变异之后，回滚空操作（提权链开放） | 把 `cred.clone()` 移到 setuid/caps 变异之前（:289 处） | S | ✅ 已修复（含 2R.14：exec 失败不再唤醒 vfork 父进程） |
| 2R.2 | NEW-C1 | syscall/memory.rs + arch mm_ops.rs:254 + mmu_init.rs map_page + io_uring/mod.rs:490 | mmap/io_uring-mmap 接受内核地址 → 覆写共享内核页表提权 | sys_mmap 与各 fixed 分支强制 `start+size <= USER_END`；map_page 增加 vpn2<256 守卫（防御纵深） | S | ✅ 已修复：sys_mmap 入口（MAP_FIXED 拒绝/hint 归零）+ map_region fixed 分支上界 + io_uring handler 独立校验 |
| 2R.3 | SYSA-C1-FIX | syscall/io.rs:95,157,262,786；file.rs:281 | 2GB 钳制仍超 32MB 内核堆 → panic | read/write/pread/pwrite 改 64KB 分块循环（拷一块处理一块，返回累计值）；getdents 保持 4MB 上限但改 try_reserve 失败转 EFAULT | M | ✅ 已修复：read/pread64/write/pwrite64 全部 RW_CHUNK=64KB 分块；getdents try_reserve_exact + 零填充（连带修复 VFS-M7 堆信息泄漏） |
| 2R.4 | NEW-C3 | syscall/file.rs:599 | readlink("/proc/N/fd") parts[3] 越界 panic | `parts.len() != 4` 判定；同函数三处裸写用户内存改 copy_to_user | S | ✅ 已修复（含 2R.5：readlinkat 经 resolve_user_path 解析 dirfd） |
| 2R.5 | readlinkat dirfd | syscall/file.rs:489-530 | readlinkat 忽略 dirfd（NEW-H） | 通用分支复用 resolve_user_path（NOFOLLOW 查找） | S | ✅ 已修复 |
| 2R.6 | ARCH-H3 回归 | ipc/sysv_shm.rs:570 + io_uring/mod.rs:505 + arch mm_ops.rs:444 | put_page 对无记账映射成灾：munmap/shmdt 释放 shm 段页与 ring 页（UAF+双释） | shmat 每次 attach 对段内页 get_page+inc_mapcount；io_uring mmap 对 ring 页 get_page（Drop 改为 put_page 到 0 才 free） | M | ✅ 已修复：shmat 每页 get_page、ShmPages::drop 改 put_page 感知；io_uring mmap 每页 get_page、free_ring_region put_page 感知 |
| 2R.7 | IOU-H1/H2-FIX | io_uring/mod.rs:426-473 | close/enter/mmap 并发 UAF + munmap→close 双释 | private_data 读写加锁（读指针+try_ref 一段、清 None+unref 一段）；mmap handler 传入已校验的 file 而非 fd | M | ✅ 已修复：模块级 LIFECYCLE_LOCK 覆盖 close/enter/register/mmap 四路径；close 先清 None 再 unref；mmap 传入已验证 &File |
| 2R.8 | 2.25-FIX | signal.rs:786-791 | 离帧检测条件反向（门控失效+新崩溃路径） | 判活条件改 `sp <= frame_addr + FRAME_SIZE`（sp 跳到帧上界之上才算 longjmp 逃逸） | S | ✅ 已修复：判活改为 sp <= frame_addr（handler 执行期 sp 恒在帧下方） |
| 2R.9 | IPC-H3-FIX | sync/futex.rs:344-397 | futex 超时永不返回 ETIMEDOUT | 醒后 `!woken && now >= deadline` → `-ETIMEDOUT` | S | ✅ 已修复 |
| 2R.10 | IPC-C4-FIX | ipc/sysv_shm.rs:249-259 | RMID 判定与释放不同临界区 | nattch==0 时在持 slots 锁内置 deleted+摘链，drop 后再 free | S | ✅ 已修复（连带 IPC-H9：RMID 即刻置 deleted；shmdt/detach/attach 走 find_including_deleted 完成延迟释放） |
| 2R.11 | VFS-H7-FIX | fs/bio.rs:704-771 | 命中不查 BH_Uptodate（在飞条目被当命中 → 脏读）+ 双检分支释放在飞 DMA 缓冲 | 命中分支检查 is_uptodate，否则走在飞挂接/等待；双检分支不再立即 from_raw 新建条目 | M | ✅ 已修复：在飞命中改等待（wait_buffer_io_done 定时睡眠轮询 BH_Req）后 complete；双检分支等自身 I/O 落地后再释放 + 等胜者缓冲就绪 |
| 2R.12 | NEW（e_phentsize） | process/exec.rs:438 + init.rs:332 + fs/elf.rs | e_phentsize≠56 时按其拷贝 → OOB 读 | 三处校验 `e_phentsize == 56` 否则 ENOEXEC | S | ✅ 已修复：Elf64Ehdr::from_bytes 统一拒绝 e_phentsize≠56（所有解析路径的公共入口） |
| 2R.13 | NEW-C2 最小集 | process/task.rs:1309 + sched/sched.rs | wake_up 可把复查后不睡的 current 任务入队（vfork/do_wait 窗口）→ 双核同跑 | wake_up 对 `task == current` 只置 RUNNING 不入队；另提供 `dequeue_self_if_enqueued()` 在 vfork/do_wait 复查决定不睡时调用（完整 on_cpu 状态机留 Wave 5.4） | M | ✅ 已修复（实现升级）：Task 增 on_grq 标志（GRQ 锁内维护，trap.S 偏移 0x00-0x18 未受影响）；enqueue/dequeue/pick 全守卫化（连带修复 P12 nr_running 双加与 P08 RT 下溢）；vfork/do_wait 复查路径接 dequeue_if_enqueued |
| 2R.14 | vfork 失败唤醒 | syscall/process.rs:387-391 | exec 失败唤醒父 → CLONE_VM 共享栈并发（NEW-H） | 删除失败路径唤醒；失败后子进程应当走 exit（POSIX vfork 语义） | S | ✅ 已修复（随 2R.1） |
| 2R.15 | NEW-H ti_cpu | sched/sched.rs:362-367 | deferred-notify 强改运行中任务 ti_cpu → cpu_id() 说谎 | set_ti_cpu 仅在目标为睡眠态时执行（或通知路径改走任务自身 CPU 记录） | S | ✅ 已修复 |
| 2R.16 | semop 累计 | ipc/sysv_sem.rs:696-727 | 多操作从同一初值计算，非累计（-1,-1 只扣 1） | 按 Linux perform_atomic_semop：累计值验证与应用 | S | ✅ 已修复：工作副本累计计算 |
| 2R.17 | verify drift | kernel/verify | 同步门禁 HEAD 上失败（17 处 drift，kani/miri CI 必红） | 同步 verify 拷贝；verify_exception_table.py 接入构建门禁 | M | ⏸ 暂缓至 Wave 8：17 处均为机械拷贝同步（vma_manager.add/TcpFlags setters 等），不阻塞内核正确性；异常表脚本已本地验证 26/0

**完成记录（2026-09-13）**：2R.1–2R.16 完成（2R.17 暂缓至 Wave 8）。验证：make build 通过、异常表 26/0、QEMU 4 核 SMP 启动到 mrsh shell。注意：2R.13 实现从"current 不入队"升级为统一的 on_grq 计账（覆盖跨 CPU 场景），并将 P08/P12 一并闭合。

**验收标准**：`mmap(0xffffffff80000000, MAP_FIXED)` 返回 EINVAL/ENOMEM 不 panic；`read(fd, buf, 0x4000_0000)` 分块成功不 panic；`readlink("/proc/1/fd")` 返回 EINVAL；半合法 setuid ELF 失败后 euid 不变；`shmat;shmdt;IPC_RMID` 与 `io_uring mmap;munmap;close` 无双重释放（buddy 完好）；futex WAIT 带超时真实返回 ETIMEDOUT。

**验收标准**：
- 权限：非 root 用户 open /etc/shadow 失败（EACCES）；O_RDONLY fd 写返回 EBADF；目录 0700 不可穿越；
- 提权：半合法 setuid ELF 加载失败后 euid 不变；setuid 动态程序忽略 LD_PRELOAD；
- 数据：mmap 写后 fork，子进程修改不影响父（COW）；mremap(MAYMOVE) 后原数据完整；munmap 后 `/proc/meminfo` 可回收；truncate→扩展→写入后文件内容正确且不串块；
- 挂死：`pthread_cond_broadcast`（>8 等待者）、`semop` 阻塞 P、`mq_timedreceive` 超时、vfork+exec、空管道 SIGKILL 全部正常返回/终止；
- LTP 相关子集（sysvipc、mmap、wait、kill）通过率提升且无挂死。

---

## 5. Wave 3 — 网络栈恢复

> 修复顺序严格按依赖（**第四轮复查修正**）：先初始化（0），再让包能出/能进（C2→C1→H1→tot_len/checksum 字节序），再修 fd 寻址（C3），再字节序深修（H2/H3/H4），再 loopback 队列化，最后重构服务器路径（C4）。每个环节修完在 loopback 上做端到端验证（此前所有"FIXED"都没有经过这一步）。
>
> **第四轮复查补充（详见检视报告 §9/§16）**：C3 实为 24 处（11 处 get_socket + 13 处 tcp/udp_socket_get）；C4 实为六处断裂（客户端 RX 不可见 + accept 返回协议表下标冒充进程 fd）；H4 漏了客户端 tcp.rs:857；新增 init 未调用、tot_len 多加 20、TCP checksum 写回缺 to_be、loopback 重入 UB 四个更高优先级断点。

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 3.0 | NEW-C4 | main.rs + tcp.rs:1495/1507 + tcp_timer.rs:136/143 + route.rs | init_tcp_manager/init_tcp_timer_manager/route_init 全库零调用（首包 panic + 每 tick UB） | 启动序列（驱动初始化后）调用三个 init | S | ✅ 已修复：main.rs 网络设备初始化块接线 |
| 3.1 | NET-C2 + M1 | net/buffer.rs:146-155 | headroom 仅 16 字节（需 54）+ 错误路径泄漏 skb | headroom 提升到 ≥128（LL_MAX_HEADER）；SkBuff 实现 Drop（或所有 Err 路径显式 free） | S | ✅ 已修复：headroom=128 + Drop（幂等 release，显式 free 调用兼容） |
| 3.2 | NET-C1 | net/ethernet.rs:359-392 | RX 从不剥以太网头，入站全丢 | dispatch 前调用 eth_pull_header()/skb_pull(ETH_HLEN) | S | ✅ 已修复：ethernet_rcv 先 eth_pull_header 再分发；未知 ethertype 不再回退 IP |
| 3.3 | NET-H1 | net/ethernet.rs:110 | ethertype 写入缺 to_be()（出站帧全被对端丢） | `h_proto = proto.to_u16().to_be()` | S | ✅ 已修复 |
| 3.3b | NEW-C5 | ipv4/mod.rs:202,213 + tcp.rs:1917 | tot_len 多加 20；TCP checksum 写回缺 to_be | `total_len = skb.len`（push 后）；`check = csum.to_be()` | S | ✅ 已修复 |
| 3.4 | NET-C3 | net/socket.rs:572 + syscall/network.rs 24 处 | 进程 fd 直接索引全局 socket 表（listen 等全 EBADF/错对象） | 全部改用 get_socket_from_fd(fd)（bind 已是范本），经 Socket 的 tcp_fd/udp_fd 访问协议表 | M | ✅ 已修复：network.rs 全部取用点改 get_socket_from_fd/tcp_proto_fd/udp_proto_fd；新增 socket_create_accepted；sendmmsg/recvmmsg 步长 64（H-09）；M5/M22/M24（shutdown 发 FIN、SHUT_RD 不再断发送） |
| 3.5 | NET-H2/H3/H4 | net/tcp.rs:1802,832,839,**857**；net/udp.rs:438 | TCP 伪头读法不一致、UDP 端口/长度本机序求和、握手 ISN 未转字节序（服务器+客户端两处） | 三处统一为 from_be_bytes 大端读法；ISN 补 TcpSeq::from_be；统一 IP 参数主机序约定 | S | ✅ 已修复（TCP/UDP 伪头半字 from_be；ISN 服务器+客户端；UDP 校验和端口/长度 from_be；RX 传 to_be 保持约定） |
| 3.5b | NEW-C6 | net/loopback.rs:43-57 + ethernet.rs:326-333 | loopback 同步重入 &mut 别名 UB | loopback_xmit 改入队 backlog，loopback_poll 真正出队，ethernet_poll 统一排空 | M | ✅ 已修复：有界 backlog（256）+ raise NetRx；ethernet_poll 先排 loopback 再 virtio；accept/recv 前 opportunistic 排空（poll-before-block）；127.0.0.0/8 TX 短路不经 ARP |
| 3.6 | NET-C4 | net/tcp.rs:1399-1485,1736-1785 | 服务器路径六处断裂（listen 不可见/SYN-ACK 不发/accept 恒 EAGAIN/accept 后吞包/客户端 RX 不可见/协议下标冒充 fd） | 单一事实来源重构：TcpSocketTable 为唯一存放处；listen_fds 注册；SYN→新槽位以 LISTEN 状态调 handle_packet；accept 标记不移除；sys_accept 建新 File+fd | L | ✅ 已修复：表扫描替代 manager 三表（LISTEN 检测+4 元组匹配含 ANY 通配）；SYN→子槽位 LISTEN 态驱动状态机；parent_fd/accepted 标记；backlog 上限 64；tcp_v4_err 改扫表；M6 临时端口（32768-60999 查重） |
| 3.7 | NET-H5/H6 | net/arp.rs:414；syscall/network.rs:85；net/udp.rs:102 | 本机 IP 硬编码 + bind 忽略 sin_addr（F10-13/20 修复无效） | 最小 netif 地址表（ip/掩码），get_local_ip 从设备读、提供配置入口；sys_bind 解析 sin_addr 传入；UdpSocket::new 的 local_ip 初始为 0（ANY） | M | ☐ |
| 3.8 | NET-M2/M3/M6/M7 | net/tcp.rs:961,706-827,586；tcp_timer.rs:86 | FIN 不校验序号、窗口完全忽略、无临时端口、SYN 无重传无 RST 感知 | FIN 仅在 seq+seg_len==rcv_nxt 时处理；ACK 时更新 snd_wnd；connect 前自动分配临时端口（bind(:0) 豁免特权检查）；timer 增加 SYN_SENT 重传+退避+ETIMEDOUT，RST 覆盖 SYN_SENT | M | ☐ |
| 3.9 | NET-M4/M5/M12 + H-09/M22/M23/M24 | net/socket.rs:244-266,339；syscall/network.rs | poll 恒不就绪（recv_queue 无生产者）、UDP send 丢 dest_addr、shutdown 不发 FIN、sendmmsg/recvmmsg 步长 60、sendmsg 丢 msg_name/recvmsg 不回填、accept 不写 addr | poll 下查 tcp/udp 实际缓冲；dest_addr 分支改调 udp_sendto；shutdown(SHUT_WR) 触发 close() 发 FIN；步长 64；msg_name 解析/回填 | M | ☐ |
| 3.10 | NET-M8/M15 + H-10 | tcp_timer.rs:55；tcp.rs:1489,1611；net/socket.rs:519 | TIME_WAIT/pending 无回收且无 backlog 上限、manager 三上下文无锁、socket 不屏蔽 SOCK_CLOEXEC/NONBLOCK | manager 与协议表统一包 Spinlock（锁内不做 TX，配合 loopback 队列化）；超时扫描与 backlog 上限；socket() 屏蔽 type flags | L | ☐ |
| 3.11 | NET-M9/M10/M13/M14 + L 系列 | ipv4/mod.rs、arp.rs、tcp.rs | ip_rcv 四项（ihl/校验和/分片/目的校验）、端口冲突、u16 窗口截断、ARP 过期与投毒、ETH_ZLEN、RST 构造等 | 按检视报告 §9 逐项跟进（from_bytes 对齐 UB 与 M15 锁一起做） | L | ☐ |

**完成记录（2026-09-13）**：3.0–3.6 完成。nettest 已并入 `test/mkrootfs.sh`（mkfs.ext4 -d 免 sudo 重写，随镜像构建现场编译进 /test/nettest，改 nettest.c 后 `./test/mkrootfs.sh` 一条命令即更新）。**新观察（既有缺陷，非本轮回归）**：全新镜像上 `/test/smoke_test` 的 lseek 用例确定性失败（14/15，write→SEEK→read 回读内容错；用 be73c63 内核实测同样失败）——旧镜像的缓存状态掩盖了它，归入 ext4 写/缓存一致性族（Wave 7，关联 EXT4-H8/bio 缓存）。
**验收实测**：新增 `test/nettest.c`（裸系统调用、无 libc 的用户态 E2E 测试，riscv64-linux-gnu-gcc -nostdlib 静态构建后注入 rootfs 的 `/test/nettest`，与 smoke_test 同目录；运行 `/test/nettest`，输出 `NETTEST PASS` 即通过）——UDP loopback echo ✓、TCP 三方握手 ✓、TCP echo ✓（"NETTEST PASS"，多次重跑稳定；第三次偶发失败为已知 UART 输入丢失，命令未送达）。调试中发现并修复两个隐藏断点：出站源 IP 恒为 arp::get_local_ip() 硬编码值（新增 ipv4_send_src，TCP/UDP 传 socket local_ip）；客户端 socket local_ip=0 导致 SYN-ACK 4 元组匹配失败（connect 时自动填充：loopback 目的→127.0.0.1，否则设备地址）。**回归教训（已记录）**：Wave 2R 的 Task.on_grq 并行记账标志与各调度类内部 on_rq 语义冲突（RT RR 轮转合法保留 current 在队），导致任务丢失/shell 无响应——已回退为依赖 CFS 自身幂等 dequeue 的 dequeue_if_enqueued（CFS-only），A/B 实测恢复。ICMP ping 程序与 slirp 场景未验证（无对应用户程序），3.7-3.11 留待后续。

**验收标准**：loopback 上 TCP 客户端/服务器 echo、UDP 收发、ICMP ping 程序可用；QEMU slirp（10.0.2.x）下 DHCP 场景 ARP/ICMP 通；LTP network 子集无挂死；`cond_broadcast`/多线程网络程序无泄漏（观察 skb 计数）。

---

## 6. Wave 4 — 驱动修复

> **第四轮复查补充**：4.4 必须连带 ECAM 扫描修正（NEW-H：`pci/mod.rs:413` 步长 0x1000 把功能位当设备号，slot 放大 8 倍——第 5 个设备起不可见且 `(pin-1+slot)%4` 公式失效）；4.7 必须先过 0x1052 ID 关（`virtio_pci.rs:193` match 不接受 VIRTIO_INPUT）；4.1 必须连带"MMIO 探测先于 ext4 挂载"（`main.rs:557` 探测晚于挂载块）与"MMIO notify 用 32 位写"（`queue.rs:227`，virtio-mmio 要求 32 位寄存器访问）；4.2 修复会触发 `virtio_net.rs:449` poll 把 DMA 物理地址当虚拟地址解引用（需 phys_to_virt）。当前 QEMU 配置只有 PCI 路径是活的（run.sh 用 virtio-blk-pci，无 MMIO/net 设备）。

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 4.1 | DRIV-C1 | drivers/virtio/mod.rs:232-268 | MMIO virtio-blk 用 PCI common-config 偏移写 MMIO 寄存器（设备死寂） | 改用 MMIO 正确偏移：QUEUE_DESC 0x80/0x84、QUEUE_AVAIL 0x90/0x94、QUEUE_USED 0xa0/0xa4、QUEUE_READY 0x44；FEATURES_OK 后回读校验；MMIO notify 改 32 位写；探测顺序移到挂载前；删除布局错误的死代码 VirtIOBlkRegs | M | ☐ |
| 4.2 | DRIV-C2 + M1/M5 | drivers/net/virtio_net.rs:187-405 | MMIO 网卡 avail/used 写 0、desc 指向死表；关中断自旋数秒假报成功；MTU 读的是 status 字段 | 删除死表，直接把 VirtQueue 内部三段物理地址写入设备；补 FEATURES_OK 流程；xmit 移出锁/关中断区并如实报错；MTU 默认 1500、协商特性后从 0x10a 读；poll 用 phys_to_virt | M | ☐ |
| 4.3 | DRIV-H2 | drivers/virtio/probe.rs:228-231 + queue.rs | 特性协商全盘接受（EVENT_IDX/INDIRECT）而 used_event 是堆垃圾 → 完成中断被抑制（"random timeout"根因） | 回写前掩掉 `!(INDIRECT|EVENT_IDX|ANY_LAYOUT)`；word1 写 VERSION_1；去掉/保留 5 次重试视修复效果 | S | ✅ 已修复：probe.rs 掩码 EVENT_IDX/INDIRECT/ANY_LAYOUT；write_driver_features 补 word1 VERSION_1 |
| 4.4 | DRIV-H3 + NEW | drivers/virtio/virtio_pci.rs:560-575 + pci/mod.rs:413 | INTX IRQ 差一条线 + 不读 ISR（电平风暴）+ ECAM slot 放大 8 倍 | `32 + ((int_pin - 1 + pci_slot) % 4)` 且 pci_slot 改 `((addr-ECAM)>>15)&0x1f`（扫描步长 0x8000）；处理程序开头读 isr_cfg_bar 按 bit 分派（读即清） | M | ✅ 已修复：IRQ 公式补 -1；pci_slot 取 ECAM>>15；ECAM 按 slot×0x8000×8 函数全扫；interrupt_handler_pci 先读 ISR（0 → NotOurs）；枚举补全 0x1041/0x1042/0x1050/0x1052 |
| 4.5 | DRIV-H1 | drivers/virtio/queue.rs:555-573 | 描述符按链回收缺失（≥3 并发复用飞行中描述符 → DMA 串包） | 每链记录头描述符与链长，完成时整链归还空闲链表；守卫改为未归还描述符数 + 需求 ≤ queue_size | M | ☐ |
| 4.6 | DRIV-H4 | drivers/virtio/mod.rs:798-805 | 异步读响应描述符缺 F_WRITE（必 EIO） | flags 改 VIRTQ_DESC_F_WRITE | S | ✅ 已修复 |
| 4.7 | DRIV-M2/M6 + NEW | drivers/input/virtio_input.rs:288-341；virtio_pci.rs:536,193 | input config 打在 common cfg 上（设备名垃圾/指针误判）；notify 地址公式多乘 2；0x1052 不被接受 | VirtIOPCI::new 加 VIRTIO_INPUT=0x1052；统一改用 device_cfg_bar；notify_off 从 common cfg Q_NOFF(0x1e) 读取，按 `base + off*multiplier` 计算 | M | ☐ |
| 4.8 | DRIV-M3/M4/M7/M8 | fbdev.rs:39；queue.rs:208；virtio_pci.rs:1084；virtio_net.rs:441 | fb_fix_screeninfo ABI 缺 4 字段；csrci sie,9 空操作；PCI 完成匹配计数制；poll 提前返回泄漏 RX 缓冲 | fbdev 补 type_aux/xpanstep/ypanstep/ywrapstep；改 `li t0,0x200; csrc sie,t0`；PCI 路径改用 wait_for_desc_completion 按描述符匹配；poll 统一收尾路径（释放+pop+refill） | M | ☐ |
| 4.9 | DRIV-L 系列 | console.rs:141 等 | UART 不查 LSR_THRE、getchar 竞态、device_status u32 读写、PLIC 无效残留、evdev 死代码等 8 项（红蓝对调已证伪撤销） | 按检视报告 §10 Low 清单逐项处理（多数 S 级） | M | ☐ |

**完成记录（2026-09-13）**：4.3/4.4/4.6 + 设备 ID 补全完成（当前 QEMU 活跃的 PCI 路径优先）。验证：make build 通过、QEMU 启动 53 项 [ok]（ext4 经修复后的 virtio-blk PCI 挂载正常——特性掩码与 VERSION_1 未破坏活路径）、nettest PASS。4.1/4.2（MMIO 路径，当前配置不激活）、4.5/4.7/4.8/4.9 留待后续批次。

**验收标准**：MMIO-only 配置（无 PCI）能挂载 rootfs 启动；virtio-net 发包不再秒级停顿；PCI virtio-blk 去掉重试后 I/O 稳定；`/dev/input` 设备名正确、鼠标注册为指针设备。

**掩盖关系**：4.6 与 4.2 的 M1 在 4.1/4.2 修复后立即暴露，必须同批完成。

---

## 7. Wave 5 — 进程/调度/信号语义

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 5.1 | PROC-P03 | process/exit.rs | do_exit 缺孤儿过继（悬挂 parent 指针 UAF + 僵尸泄漏） | exit 前持 PROCESS_TREE_LOCK 遍历 children 改挂 init/reaper，僵尸孩子补发 SIGCHLD | M | ☐ |
| 5.2 | PROC-P05 | process/exec.rs:395-542 | AT_RANDOM 指向 AT_NULL 且覆写 AT_PHDR（canary 恒 0） | phdr_offset = random_offset + 4；total_slots 两处同步补 2 槽 | S | ✅ 已修复（根因比报告更深）：musl ld.so 以 `AT_PHDR − PT_PHDR.p_vaddr` 推算主程序装载基址（dynlink.c:1843），Linux 语义要求 AT_PHDR 指向**映像内**的 phdr 表；本内核把它指向栈拷贝 → PIE 基址被算成栈地址 → 重定位全错 → ld.so 静默死亡（rc=129）。旧 bug 的随机数覆写恰好打掉 PT_PHDR 项使 musl 匹配不到、基址保持 0（本内核 PIE 恰以 0 为基址）从而长期掩盖。修复：PT_PHDR 存在时 AT_PHDR=p_vaddr（段按 p_vaddr 映射、bias=0），栈拷贝仅作无 PT_PHDR 时的回退；AT_RANDOM 移至 AT_NULL 之后的独立 16 字节（真随机数，canary 不再恒 0）；尺寸计算同步 +4 槽。验证：dynamic_link_test OK（rc=0）、smoke 15/15、nettest 4/4 |
| 5.3 | PROC-P06 + syscallb-M12/M13 | sched/rt.rs:19；syscall/sched.rs | RT 优先级方向相反 + 无取值校验 + getpriority 编码错（nice+20 应为 20-nice） | syscall 层校验 [1,99] 并做方向转换（存 99-prio）；getpriority 按 `20 - nice` 返回 | S | 🔧 P06 完成（位图索引反转：bit0=优先级 99；RT 取值校验 [1,99]、非 RT 必须 0、上 RT 需 CAP_SYS_NICE 含自身——H-12 一并闭合）；M12 getpriority 编码待修 |
| 5.4 | PROC-P07/P08/P12 + NEW-C2 | syscall/sched.rs + sched/rt.rs + task.rs | 改策略不做队列迁移（双队列灾难/挂死）；RT 退出计数下溢；wake_up 非原子 + 无 on_cpu 守卫（vfork/do_wait 窗口双核同跑，第四轮新 Critical） | 引入 on_rq/on_cpu 状态机：wake 对 current 只置位不入队、enqueue/dequeue 全部持 GRQ 锁原子完成、grq.nr_running 与类队列同临界区；持 GRQ 锁按旧策略 dequeue → 改字段 → 新策略 enqueue；rt_rq.dequeue 补 is_on_rq 守卫；去掉 do_exit/__schedule 双重出队 | L | ☐ |
| 5.4b | NEW-H（信号 ABI） | kernel/src/signal.rs:152-206 + syscall/signal.rs | rt_sigaction 结构布局与 RISC-V ABI 错位（内核 24B {handler,flags,mask} vs ABI 32B {handler,flags,restorer,mask}——sa_mask 读到 restorer 指针，oldact 覆写 sa_restorer） | 定义用户侧 SigActionUser 布局（32B），copy_from/to_user 按此转换；sa_restorer 不经内核语义只透传 | M | ✅ 已修复：SigActionUser 32B 转换 + sa_restorer 字段透传；设置时从 sa_mask 剥离 SIGKILL/SIGSTOP 位 |
| 5.5 | PROC-P09 | sched/fair.rs + sched.rs | 全局 cfs_rq.curr 在 4 核记账错乱（公平性失真） | update_curr 按传入目标任务（prev）记账而非 self.curr；或维护 per-CPU curr 数组 | L | ☐ |
| 5.6 | PROC-P10 + syscallb-H12 | syscall/sched.rs:136,522 | setpriority 缺 can_nice；sched_setattr/setscheduler 对自身提权 RT 无检查 | 统一：降 nice 值/升 RT 无论目标是否自身都要求 CAP_SYS_NICE | S | ☐ |
| 5.7 | syscallb-H02/H03 | syscall/process.rs:543,1665 | kill(-1) 语义；getcpu 每指针写 16 字节溢出 | kill(-1) 遍历全部（跳过 self/pid1）；getcpu 每指针只写一个 int | S | 🔧 H-03 完成（getcpu 每指针 4 字节、走异常表拷贝）；H-02 kill(-1) 已随 Wave 2 修复 |
| 5.8 | syscallb-H06/H07 | syscall/signal.rs:396；process.rs:1595 | sigsuspend 恢复掩码过早（信号永不投递）；sigtimedwait 不阻塞/不出队（sigwait 全坏） | sigsuspend 检测到信号时先走投递再恢复旧掩码；sigtimedwait 改为 pending&set 取最低位并出队、无信号按超时睡眠 | M | 🔧 H-06 完成（仿 TIF_RESTORE_SIGMASK：sigsuspend 记录待恢复掩码，投递前在 check_and_deliver_signals 恢复，exec 清标志）；H-07 待修 |
| 5.9 | 信号 M 系列 | signal.rs + syscall/signal.rs | sa_mask 不生效、siginfo 恒 SI_KERNEL、SigContext 布局错位 8 字节、sigreturn 从内核备份恢复、SigInfo union 偏移错 | setup_frame 应用 `sigmask |= action.sa_mask`；siginfo 按发送者填写并修 union 偏移（si_uid@20、status@24）；FP 状态保存/恢复；sigreturn 改从用户帧 copy_from_user 读回并校验 | L | ☐ |
| 5.10 | PROC-P12/P13/P14 | sched/sched.rs:855；exit.rs:630 | wake_up 非原子（nr_running 膨胀/DL 双插）；waitid 漏复查；wait4 的 pid==0/pid<-pgid 未实现 | wake_up 用 state CAS 裁决；enqueue 仅在子队列成功时 fetch_add；DL enqueue 补 on_rq 守卫；do_waitid 复制 do_wait 复查块；do_wait 过滤补 pgid 两分支 | M | ☐ |
| 5.11 | PROC-P18/P19/P22/P23/P24 | fork.rs；task.rs；kthread.rs；exit.rs | clone 失败泄漏 PID+野指针；fork 不继承调度属性/comm；kthread 无主僵尸；构造漏初始化字段；release_task 不运行 Drop | 统一 destroy_new_task 帮助函数（pid_hash_remove→free_pid→栈→slot）；复制 nice/policy/affinity/comm；kthread 挂 init 或 self-reap；构造补 comm/kernel_stack_bottom/ti_a0-2 初始化；release 前 set_exe_path(b"")+pending.clear() 或 ptr::drop_in_place | M | ☐ |
| 5.12 | 其余 P/信号 Low | 检视报告 §4/§6 | P15-P17/P25-P28/P29-P35 与信号 Low 共 ~20 项 | 按检视报告逐项跟进（多数 S 级，可合并提交） | L | ☐ |

**验收标准**：`posix_spawn`/vfork 密集循环无挂死；shell 后台任务在 shell 退出后由 init 收养并 reap（PID 不泄漏）；`chrt -f 50` 优先级语义与 Linux 一致；两个使用浮点的进程交叉运行结果正确；musl sigwait/sigsuspend 测试通过；`getrusage`/`nice`/`ulimit` 行为正确。

---

## 8. Wave 6 — 内存语义完善

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 6.1 | MM-H5/H6 | arch/mm/mm_ops.rs:1023,520 | fork 不复制 swap entry（子进程得零页）；VMA 快照丢 offset；SHM/SHARED 页被错误 COW | V=0 且 is_swap_entry 的 PTE 原样复制；快照元组加 offset；fork 前按 VMA 判断 SHARED 跳过 W 降级 | M | ☐ |
| 6.2 | MM-H7 | syscall/memory.rs:493 | mprotect 重建 PTE 丢 COW 位且 PROT_WRITE 直接置 W | 保留原 PTE 的 COW(bit8)/G 位；原 COW=1 时不置 W（写错误由缺页处理） | S | ☐ |
| 6.3 | MM-H1 | mm/vma.rs:346 | VMA 合并忽略 file_fd/offset（相邻不同文件 mmap 合并 → 读错数据） | can_merge 增加文件一致性校验；文件映射稳妥起见不参与合并 | S | ☐ |
| 6.4 | ARCH-M3 + SYSA-H1 同源 | arch/mm/mm_ops.rs:374-409 | munmap 不支持 VMA 分割、跨 VMA 只删第一个（"复活"为零页/ENOMEM） | VmaManager 实现 split（前/中/后）；munmap 循环移除所有相交部分的相交页区间 | M | ☐ |
| 6.5 | MM-M2/M3 | mm/kswapd.rs；page_fault.rs | kswapd 丢唤醒；匿名页不设 SwapBacked 不入 LRU（有 swap 也不回收 → 直接 OOM） | prepare_to_wait 模式；缺页两处统一走 page_add_anon_rmap（设 flag+入 LRU） | S | ☐ |
| 6.6 | MM-M5/M8 | mmu_init.rs:362；page_alloc.rs:80 | 退出不清 swap entry（slot 泄漏）；分配失败无 direct reclaim | 退出路径对 V=0 swap entry 调 swap_free_slot；__alloc_pages 失败路径同步 try_to_free_pages 后重试一次 | S | ☐ |
| 6.7 | ARCH-M5 | arch/mm/mm_ops.rs:1084 | fork COW 降级仅本地 sfence（其他 CPU 残留可写项） | 降级完成后对父 mm 广播 TLB 失效（复用 smp_call_function + sfence） | S | ☐ |
| 6.8 | MM-M1/M6/M7/M9-M12 + Low | 检视报告 §3 | expand_downwards 检查范围、swap 区位置校验、init_from_memblock 实参、type 编码重叠、&mut 别名、OOM 回滚、memblock superset 等 | 逐项跟进（多数 S 级）；M7 的 layout 双源问题建议一并收敛到 config | L | ☐ |

**验收标准**：fork+压力+swap 场景下无数据交叉污染；`malloc` 大块 mmap/munmap 循环内存占用稳定（无泄漏）；mprotect 对 COW 区不破坏语义；启用 swap 后 kswapd 能正常回收（不再直接 OOM）。

---

## 9. Wave 7 — ext4 加固 + syscall 语义扫尾

| # | ID | 文件 | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|---|
| 7.1 | EXT4-H1 + M10 | fs/ext4/mod.rs:155 + bio.rs | fs 块大小≠4096 读写错位毁盘；块号忽略 first_data_block | 挂载强制 block_size==4096 否则 EIO（bio 固定 4096 前提下）；块号公式补 first_data_block | S | ☐ |
| 7.2 | EXT4-C2 | fs/ext4/mod.rs:1457-1485 | truncate 释放块不删 extent 项（跨文件块别名） | 截断同步收缩/删除内存 extent 项（ee_len/eh_entries）并 write_inode；拒绝复用带越界旧 extent 的区间 | M | ✅ 已修复：在 60 字节副本上重建 extent 数组（整段越界删除/跨界收缩 ee_len），回写 i_block；eh_entries 钳制 ≤4 |
| 7.3 | EXT4-C3 | fs/ext4/namei.rs:1553 | 快速符号链接 unlink 释放字符串字节当块号 | free_inode_blocks 增加 `is_symlink() && i_blocks==0` 直接返回 | S | ✅ 已修复：`ln -s /bin/sh x; rm x` 实测无位图损坏 |
| 7.4 | EXT4-C4 + M2 | fs/ext4/file.rs:250-281 | extent 文件洞写入覆写 extent 头；写路径采信磁盘 eh_max | 洞写入分支判 has_extent() 走 extent 插入（未实现前返回 EIO）；eh_max 钳制 ≤4（根）/≤(bs-12)/12 | M | ✅ 已修复（EIO 守卫；真 extent 插入留后续） |
| 7.5 | EXT4-H3 | fs/ext4/allocator.rs:474 | InodeAllocator off-by-one（覆写活 inode 槽位） | 返回值 +1；建议删除重复实现统一走 namei 的 ext4_new_inode | S | ✅ 已修复（+1，与 namei/释放路径约定一致） |
| 7.6 | EXT4-H4 | fs/ext4/namei.rs:354 | write_group_descriptor 对 32 字节 GD 越界写 64 字节 | 仅写 `min(desc_size, 64)` 字节 | S | ✅ 已修复（含块尾溢出守卫；当前镜像 desc_size=64） |
| 7.7 | EXT4-H5 | fs/ext4/namei.rs:1401 | is_dir_empty 每块重置计数（rmdir 删非空目录） | entry_count 移到块循环外 | S | ✅ 已修复 |
| 7.8 | EXT4-H6/H7/H8 | file.rs:176；namei.rs:1703；unlink/rmdir/rename | 预读索引错位（页缓存张冠李戴）；rename 过期快照回写；删除不失效页缓存 | 收集阶段记录真实页号再插入；rename 末尾重读父 inode 再改链接计数回写；free_inode 前 invalidate_inode(ino) | M | 🔧 H8/H6 完成（H6：预读记录真实页号数组再按其插入）；H7 留后续 |
| 7.9 | EXT4-H9 + jbd2 M7/M8 | journal.rs:50；commit.rs:147；transaction.rs:101 | 内部日志线性映射未钳制（提交越界毁盘）；tag v1 格式与 64bit 日志不兼容；事务记账失效 | j_last 用日志 inode i_size 钳制；j_superblock 赋值并按特性选 tag 布局；start handle 扣减 j_free、环绕前强制 checkpoint | M | ☐ |
| 7.10 | EXT4-H10 | fs/ext4 全模块 | SMP 已启用但 ext4 无并发防护（journal 全局 handle、位图 RMW） | 短期：per-fs 一把粗粒度互斥锁覆盖全部变更路径；journal handle 移入 task_struct | M | ☐ |
| 7.11 | EXT4-M1/M3-M9 + L 系列 | 检视报告 §8 | read_inode lo-only/172 字节强转、feature 白名单、ftruncate 上限、rec_len 校验、rename 覆盖不释放块、block[14]、其余 Low | 逐项跟进：incompat 白名单（0x40/0x80 之外拒绝）、`new_size` 上限 EFBIG、rec_len ∈ [8, bs-offset] 等 | L | ☐ |
| 7.12 | SYSA-H3/H5/H6/H7 + M 系列 | syscall/file.rs、io.rs、memory.rs | dirfd（同 2.3）、PROT_EXEC→RWX、epoll_pwait2 timespec、splice 三缺陷、23 项 Medium（见检视报告 §5） | mmap 先构造 VmaFlags 再用已有 to_page_perm()；pwait2 独立实现读 timespec；splice 走 copy_from_user+保存恢复 f_pos；M 系列逐项 | L | ☐ |
| 7.13 | syscallb M 系列 + IOU M 系列 | 检视报告 §6/§12 | clock_nanosleep ABSTIME、timer ID 复用、eventfd/timerfd 阻塞与泄漏、mq 系列专名竞态/notify 语义等 | 逐项跟进 | L | ☐ |

**完成记录（2026-09-13）**：7.2–7.7 + 7.8 的 H8 部分完成。**过程中发现并修复一个未列入报告的新缺陷**：VFS icache 的 `icache_add` 对同键条目"已存在即返回"（不替换），且 path_lookup 会用 icache 条目复活陈旧 Inode——inode 号被复用后，新文件经由旧文件的 sb 块映射读写（`ln -s x; rm x` 后新文件回读到 0x01 起头的位图内容，已实测复现）；常规文件复用此前"碰巧正确"是因为旧块恰好被重新分配。修复：free_inode 统一调用 icache_remove + invalidate_inode。**验收**：`ln -s; rm` 后文件回读 ✓、truncate 保留前段数据 ✓（nettest 新增 file/trunc 用例）、smoke 15/15（"concurrent file read" 偶发失败为既有 SMP 竞态族，Wave 5）、e2fsck -fn 仅 3 个中断残留孤立 inode、无位图/块账目错误。

**验收标准**：e2fsck 检查 Rux 写入后的镜像无错误（重点：truncate/symlink/rename 场景）；损坏镜像模糊测试（位翻转 superblock/GD/extent 头）不 panic；LTP fs 子集通过率提升。

---

## 10. Wave 8 — 测试与 CI 治理

| # | ID | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|
| 8.1 | TEST-C2 | 825 个内核单测无 CI、无自动判定 | test-ci 模式：`timeout 600 qemu | tee serial.log` + grep ALL TESTS PASSED/TESTS FAILED 定退出码；内核测试结束用 SBI shutdown 退出；新增 unit-test workflow | M | ☐ |
| 8.2 | TEST-C1 | SPIN LTL 仅默认 claim 被验证（声称 8 实际 ~3） | Makefile 循环对每个 claim `./pan -a -N <name>`；文档改实际数字 | S | ☐ |
| 8.3 | TEST-H1/H2/H3 | 假测试：tcp_handshake.rs、mem_cow.rs 整文件恒真；smp/boundary/wait4/syscall_io/syscall_time/fork 等 ≥40 断言点 | 恒真断言改真实调用或删除；兜底 else 分支一律 test_fail/test_skip；无条件 pass 删除 | M | ☐ |
| 8.4 | TEST-H4 | 测试间共享状态污染（setuid 不可恢复、fork 不回收、执行顺序耦合） | 每测试 setup/teardown；uid/pid 类测试结尾恢复全局状态；harness 增加"测试后全局状态不变"断言 | M | ☐ |
| 8.5 | TEST-H5 | Kernel.toml 多段静默无效；9 个 rustc-env 死配置；.config 回退硬编码 | build.rs 增加 unknown-key 警告；.config 缺失键回退 Kernel.toml；删除或实现无效段；release 显式开启 overflow-checks | M | ☐ |
| 8.6 | TEST-C3 | 测试数字三套矛盾 | 写统计脚本（test_pass/test_fail/assert 计数 + verify #[test] 计数）自动生成文档数字 | S | ☐ |
| 8.7 | 补测 | Wave 1/2 修复项缺回归测试 | 为坏指针/坏 ELF/坏镜像/权限/COW/vfork/semop/pipe 中断各写最小回归用例（并入 smoke 或单测） | M | ☐ |
| 8.8 | TEST-M 系列 | 构建配置 10 项（profile 无效、工具链未锁定、boot.S 硬编码编译器、生成文件入库、链接脚本路径假设、8MB early 映射无断言等） | 逐项跟进；linker.ld 加 `ASSERT(_end - KERNEL_VIRT <= 8MB)` | L | ☐ |

---

## 11. Wave 9 — 文档治理

| # | ID | 问题 | 修复方案 | 工作量 | 状态 |
|---|---|---|---|---|---|
| 9.1 | kernel-lock.md + riscv64.md + roadmap.md | 整份描述已删除的 Kernel Big Lock 为现行机制 | 重写为现行同步机制概览（preempt_count+细粒度锁+锁序）或移入 archive 标注"已移除"；riscv64.md 删大锁三段；roadmap 删特性行 | S | ☐ |
| 9.2 | CLAUDE.md | 3 个失效链接、Phase 36、203 tests | 全面更新为当前状态（Phase 52、正确路径、verify 工作流） | S | ☐ |
| 9.3 | README.md | 348→344 syscalls、测试数不自洽、行数多版本、启动日志与代码相反 | 用 8.6 的脚本重新生成数字；启动日志换真实串口输出 | S | ☐ |
| 9.4 | getting-started.md / development.md / debugging.md | mini-ltp/RELEASE=1/distclean/quick_test.sh/bin/shell 失效；持久日志描述与代码相反 | 删除或改有效命令；持久日志注明默认禁用 | S | ☐ |
| 9.5 | configuration.md | 默认值错误 3 处、段落归属错、40+ 配置项缺失 | 对照 Kernel.toml 全面修订 | M | ☐ |
| 9.6 | structure.md | 不存在的测试文件/mutex.rs、行数过时 | 用脚本重生成清单 | S | ☐ |
| 9.7 | 36 处失效链接 + verify/README 数字 + archive 索引 | 批量修复；archive/README.md 补 30 份文档索引与时点标注 | S | ☐ |
| 9.8 | 缺失文档 | net/ipc/security/swap/io_uring/信号/POSIX timer 无设计文档 | 按 memory.md 的标准（与代码逐项核验）补齐，优先 net 与 io_uring | L | ☐ |

---

## 12. 依赖与掩盖关系（修复时必须注意）

1. **ARCH-C1（异常表）是 1.6 裸解引用修复的前置**：异常表修复后，漏改的裸解引用才从"整机挂死"降级为 EFAULT，仍需全部改 copy_from_user。
2. **DRIV-C1/C2 修复会暴露** DRIV-H4（async 读必 EIO）与 DRIV-M1（MTU=1）——必须同批修。
3. **NET 已知限制（F10-06/07/22/24/29/33）全部被 NET-C1/C2/C3/C4 掩盖**：修完 C 系列后再评估已知限制清单是否仍成立（预计 F10-06/33 消解，F10-22/29 需真正实现）。
4. **ARCH-C2 修复可能改变"概率性"症状基线**：修复后请完整回归 SMP 启动、LTP 长跑，观察是否还有残留概率性问题（可能是 IPC-C2/H4 等并发缺陷的表现）。
5. **EXT4-H10（全局锁）建议在 EXT4-C2/C4 之前落地**：先消并发再改 extent 逻辑，避免改完又引入新竞态。
6. **PROC-P04（deferred notify 回归）与 PROC-P02（vfork）都在最近 commit 中引入**：修复时在 git 历史上 revert 对照验证，确认不破坏 994b306 原本要修的 lost-wakeup。

## 13. 每批次通用回归门禁

1. `make build && make build-release`（两种 profile）
2. `make test`（单测，配合 8.1 的 CI 化判定）
3. `make run` + smoke 15 项
4. 涉及 fs/ext4：e2fsck -f 校验测试后镜像
5. 涉及 net：loopback echo + slirp ping
6. 涉及 arch/sched：4 核 SMP 启动 + LTP 抽样长跑（重点观察历史"概率性"症状）
7. commit 规范沿用现有惯例：`fix(<scope>): <摘要> (Wave N)`，正文引用检视报告 ID（如 `Fixes ARCH-C1`），便于双向追溯。

## 14. 长期机制建议（防复发）

1. **异常表/汇编级 CI 校验**：构建脚本比对 .ex_table 表项与反汇编访存指令一一对应（ARCH-C1 类缺陷自动拦截）。
2. **端到端冒烟**：把"真实收发"（网络 echo）、"真实 COW"（fork+写）、"真实阻塞"（pipe/sem/futex 超时）纳入 smoke——本轮证明只测常量的单测会给 出"已修复"的假象。
3. **模糊测试入口**：ELF 头、ext4 镜像、DTB 三个解析器接入简易 fuzz（位翻转 + 边界值生成），任何 panic 即失败。
4. **数字自动生成**：测试数/代码行数/syscall 数由脚本在 CI 中生成并校对文档，杜绝多版本漂移。
5. **KNOWN LIMITATION 复审**：每轮修复后复审已知限制清单，被掩盖的项（如本轮 NET 系列）及时重新定级。
